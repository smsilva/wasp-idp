node_modules/
.yarn/cache/
.yarn/install-state.gz
dist/
dist-types/
coverage/
*.local
.env
*.env

# Personal/local context (not versioned)
CLAUDE.local.md
HANDOFF.local.md
.mcp.json

# Terraform — .terraform.lock.hcl É versionado (fixa o hash do provider)
**/.terraform/*
*.tfstate
*.tfstate.*
*.tfplan
crash.log
crash.*.log
terraform.tfvars
!terraform.tfvars.example
# Valores locais de identidade: um arquivo, carregado por cada raiz via symlink values.auto.tfvars.
# O .example É versionado — é o inventário de o que precisa ser preenchido.
aws/terraform/variables/values.tfvars
**/values.auto.tfvars
# Logs de apply/destroy: carregam account id, ARN e endpoint reais
logs/
# Metadata da aplicação SAML do Identity Center: identifica a instância de quem roda.
# Baixado do console — a API CreateApplication não cria aplicação SAML. A fixture de teste
# em tests/fixtures/ É versionada: é sintética e o provider exige >= 1000 caracteres.
saml-metadata.xml
!**/tests/fixtures/saml-metadata.xml
!**/saml-metadata.xml.example

# Andaime de contexto derivado de docs versionadas — nao versionar (segunda fonte de verdade)
docs/superpowers/context/
