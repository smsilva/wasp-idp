apiVersion: v2
name: echo
description: >-
  Smoke aberto da plataforma-base EKS (fatia 2). httpbin exposto SEM
  autenticação via istio Gateway/VirtualService + NLB, TLS pelo cert-manager
  (ClusterIssuer LE staging do ingress/TLS) e DNS pelo external-dns. Camada de
  APLICAÇÃO: instalado por helm puro, FORA do Crossplane (ADR decisão 5).
type: application
version: 0.1.0
appVersion: latest
