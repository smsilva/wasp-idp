apiVersion: v2
name: hub
description: >-
  Rede de conectividade regional (papel topológico: hub) — provisiona 1 XR Network na conta
  `network` (providerConfigName: network) e bloqueia via Job hook até ficar Ready. Um por região.
  Camada de rede; não é EKS. Ver ../../README.md.
type: application
version: 0.1.0
appVersion: "0.1.0"
